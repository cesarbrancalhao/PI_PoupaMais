export interface UserConfigs {
    tema: boolean;
    idioma: string;
    moeda: string;
  }
  
  export interface UpdateConfigsRequest {
    tema: boolean;
    idioma: string;
    moeda: string;
  }
  